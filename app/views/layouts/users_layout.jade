!!! 5
html
  head
    title= title
    != stylesheet_link_tag('bootstrap', 'style', 'bootstrap-responsive')
    != javascript_include_tag('https://ajax.googleapis.com/ajax/libs/jquery/1.8.3/jquery.min.js', 'vendor')
    != csrf_meta_tag()
  body
    .navbar
        .navbar-inner
            .container
               a.brand(href='#')  Fire

    .container
      - var flash = request.flash('info').pop(); if (flash) {
        .alert.alert-block.alert-info= flash
      - }

      - flash = request.flash('error').pop(); if (flash) {
        .alert.alert-block.alert-error= flash
      - }

      != body

      hr
      footer
        - var date = new Date()
        p © Company #{date.getFullYear()}
